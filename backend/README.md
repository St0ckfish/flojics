# Backend

Laravel 11 API for ticket escalation.

```bash
cp .env.example .env
php artisan key:generate
composer install
php artisan migrate
php artisan serve
```

Quality:

```bash
composer lint
composer analyse
composer test
```

See the repository root `README.md` and `docs/` for the planned architecture.
